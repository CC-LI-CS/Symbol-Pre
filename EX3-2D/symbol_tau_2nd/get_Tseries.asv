% function toe=get_Tseries(alpha,msize)
alpha,msize

toe=zeros(msize+1,1);toe(1)=-1;
w=zeros(msize+1,1);w(1)=alpha/2;
for k=2:msize+1
   toe(k)=(1-(alpha+1)./(k-1))*toe(k-1); 
    w(k)=(alpha/2)*toe(k)+(2-alpha)/2*toe(k-1);
end
toe=w
%% 

w=[];
w(1)=r/2;
w0=r/2
w1=(2-r-r^2)/2
w2=r*(r^2+r-4)/4
ww=w0+w2
toe=[];toe(1)=1;
for k=2:N+1
   toe(k)=(1-(r+1)./(k-1))*toe(k-1);
   w(k)=(r/2)*toe(k)+(2-r)/2*toe(k-1);
end
toe
% sum(w)
cT=-[w(2:end)]'
rT=-[w(2) w(1) zeros(1,N-2)]
T=toeplitz(cT,rT)

end